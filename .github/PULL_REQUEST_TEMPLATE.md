## What changed

<!-- One paragraph. Which skill, which file, what a user sees differently. -->

## Why

<!-- The post, rule, icon or lint result that was wrong before. Include the input that broke it. -->

## Checks

- [ ] `python3 tests/validate_skill.py` passes
- [ ] `python3 tests/test_check_blog.py` passes (if `check_blog.py` changed, add a fixture line)
- [ ] `./install.sh` re-run locally, skill still loads
- [ ] No account ids, ARNs, hostnames or customer names in any file
